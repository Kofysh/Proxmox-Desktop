<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ProxmoxDesktop.Controls.MachineCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:ProxmoxDesktop.Api.Models">

    <Grid Width="175" Height="195" Padding="0" CornerRadius="8"
          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Status badge -->
        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,8,0"
                CornerRadius="4" Padding="6,2" Background="{x:Bind StatusBrush, Mode=OneTime}">
            <TextBlock Text="{x:Bind Machine.Status, Mode=OneWay}" FontSize="10" Foreground="White"
                       CharacterSpacing="75" FontWeight="SemiBold"/>
        </Border>

        <!-- Node badge -->
        <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8,8,0,0"
                CornerRadius="4" Padding="6,2"
                Background="{ThemeResource SystemFillColorNeutralBackground}">
            <TextBlock Text="{x:Bind Machine.NodeName, Mode=OneWay}" FontSize="10"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Border>

        <!-- VM/LXC icon -->
        <Image Grid.Row="0" Source="{x:Bind MachineIcon, Mode=OneTime}"
               Width="72" Height="72" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,16,0,0"/>

        <!-- CPU / RAM bars -->
        <StackPanel Grid.Row="0" VerticalAlignment="Bottom" Padding="10,0,10,4" Spacing="4">
            <Grid ColumnDefinitions="Auto,*">
                <TextBlock Grid.Column="0" Text="CPU" FontSize="10" Width="28"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <ProgressBar Grid.Column="1" Value="{x:Bind Machine.CpuPercent, Mode=OneWay}" Maximum="100" Height="4"/>
            </Grid>
            <Grid ColumnDefinitions="Auto,*">
                <TextBlock Grid.Column="0" Text="RAM" FontSize="10" Width="28"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <ProgressBar Grid.Column="1" Value="{x:Bind Machine.MemPercent, Mode=OneWay}" Maximum="100" Height="4"/>
            </Grid>
        </StackPanel>

        <!-- Name + VMID -->
        <StackPanel Grid.Row="1" Padding="10,6,10,10" Spacing="2">
            <TextBlock Text="{x:Bind Machine.Name, Mode=OneWay}" FontWeight="SemiBold" FontSize="13"
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Text="{x:Bind Machine.Vmid, Mode=OneWay}" FontSize="11"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Context menu -->
        <Grid.ContextFlyout>
            <MenuFlyout>
                <MenuFlyoutItem Text="NoVNC"   Command="{x:Bind OpenNoVncCommand}"  IsEnabled="{x:Bind Machine.IsRunning, Mode=OneWay}">
                    <MenuFlyoutItem.Icon><FontIcon Glyph="&#xE8A7;"/></MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="xTermJS" Command="{x:Bind OpenXtermCommand}"  IsEnabled="{x:Bind CanOpenXterm, Mode=OneWay}">
                    <MenuFlyoutItem.Icon><FontIcon Glyph="&#xE756;"/></MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="SPICE"   Command="{x:Bind OpenSpiceCommand}"  IsEnabled="{x:Bind Machine.IsRunning, Mode=OneWay}">
                    <MenuFlyoutItem.Icon><FontIcon Glyph="&#xE7F4;"/></MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutSubItem Text="Power">
                    <MenuFlyoutItem Text="Start"    Command="{x:Bind StartCommand}"    IsEnabled="{x:Bind CanStart,          Mode=OneWay}"/>
                    <MenuFlyoutItem Text="Shutdown" Command="{x:Bind ShutdownCommand}" IsEnabled="{x:Bind Machine.IsRunning, Mode=OneWay}"/>
                    <MenuFlyoutItem Text="Reboot"   Command="{x:Bind RebootCommand}"   IsEnabled="{x:Bind Machine.IsRunning, Mode=OneWay}"/>
                    <MenuFlyoutItem Text="Suspend"  Command="{x:Bind SuspendCommand}"  IsEnabled="{x:Bind CanSuspend,        Mode=OneWay}"/>
                    <MenuFlyoutItem Text="Resume"   Command="{x:Bind ResumeCommand}"   IsEnabled="{x:Bind CanResume,         Mode=OneWay}"/>
                    <MenuFlyoutItem Text="Hibernate" Command="{x:Bind HibernateCommand}" IsEnabled="{x:Bind CanSuspend,      Mode=OneWay}"/>
                    <MenuFlyoutItem Text="Force Stop" Command="{x:Bind StopCommand}"   IsEnabled="{x:Bind Machine.IsRunning, Mode=OneWay}"/>
                    <MenuFlyoutItem Text="Reset"    Command="{x:Bind ResetCommand}"    IsEnabled="{x:Bind CanReset,          Mode=OneWay}"/>
                </MenuFlyoutSubItem>
            </MenuFlyout>
        </Grid.ContextFlyout>
    </Grid>
</UserControl>
