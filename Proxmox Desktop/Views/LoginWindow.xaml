<Window
    x:Class="ProxmoxDesktop.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:converters="clr-namespace:ProxmoxDesktop.Converters"
    Title="Proxmox Desktop"
    Width="440" SizeToContent="Height"
    WindowStartupLocation="CenterScreen"
    ResizeMode="NoResize"
    Background="{DynamicResource MaterialDesign.Brush.Background}"
    TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
    FontFamily="{md:MaterialDesignFont}"
    md:ThemeAssist.Theme="Inherit">
    <Window.Resources>
        <converters:NotNullToVisibilityConverter     x:Key="NotNullToVisibility"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <BooleanToVisibilityConverter               x:Key="BoolToVisibility"/>
    </Window.Resources>
    <md:DialogHost Identifier="LoginDialog">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40,32,40,40" Width="360">
                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,28">
                    <Image Source="/Assets/proxmox.png" Width="72" Height="72" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <TextBlock Text="Proxmox Desktop" Style="{StaticResource MaterialDesignHeadline5TextBlock}" HorizontalAlignment="Center" FontWeight="Medium"/>
                    <TextBlock Text="Connect to your Proxmox VE server" Style="{StaticResource MaterialDesignCaptionTextBlock}" HorizontalAlignment="Center" Opacity="0.6" Margin="0,4,0,0"/>
                </StackPanel>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="90"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" md:HintAssist.Hint="Server address" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Server, UpdateSourceTrigger=PropertyChanged}" LostFocus="Server_LostFocus"/>
                    <TextBox Grid.Column="2" md:HintAssist.Hint="Port" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" LostFocus="Server_LostFocus"/>
                </Grid>
                <TabControl Margin="0,8,0,16" Style="{StaticResource MaterialDesignTabControl}">
                    <TabItem Header="PASSWORD">
                        <StackPanel Margin="0,16,0,0">
                            <ComboBox md:HintAssist.Hint="Realm" Style="{StaticResource MaterialDesignFloatingHintComboBox}" ItemsSource="{Binding Realms}" SelectedItem="{Binding SelectedRealm}" DisplayMemberPath="Realm" Margin="0,0,0,12"/>
                            <TextBox md:HintAssist.Hint="Username" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                            <PasswordBox x:Name="PasswordBox" md:HintAssist.Hint="Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,0,0,12"/>
                            <TextBox md:HintAssist.Hint="TOTP code" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Otp, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding TotpVisible, Converter={StaticResource BoolToVisibility}}"/>
                        </StackPanel>
                    </TabItem>
                    <TabItem Header="API TOKEN">
                        <StackPanel Margin="0,16,0,0">
                            <TextBox md:HintAssist.Hint="Token ID (user@realm!tokenid)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding TokenId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                            <PasswordBox x:Name="TokenSecretBox" md:HintAssist.Hint="Token Secret" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" PasswordChanged="TokenSecretBox_PasswordChanged" Margin="0,0,0,12"/>
                            <TextBlock Style="{StaticResource MaterialDesignCaptionTextBlock}" TextWrapping="Wrap" Opacity="0.6" Text="Datacenter → Permissions → API Tokens. Secret is never saved."/>
                        </StackPanel>
                    </TabItem>
                </TabControl>
                <CheckBox Content="Ignore SSL certificate" IsChecked="{Binding SkipSsl}" Margin="0,0,0,16"/>
                <md:Card Visibility="{Binding ErrorMessage, Converter={StaticResource NotNullToVisibility}}" Background="{DynamicResource MaterialDesign.Brush.Secondary}" Padding="12,8" Margin="0,0,0,16" UniformCornerRadius="6">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Kind="AlertCircleOutline" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource MaterialDesign.Brush.Secondary.Foreground}"/>
                        <TextBlock Text="{Binding ErrorMessage}" Style="{StaticResource MaterialDesignBody2TextBlock}" Foreground="{DynamicResource MaterialDesign.Brush.Secondary.Foreground}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                    </StackPanel>
                </md:Card>
                <Button Command="{Binding LoginCommand}" Style="{StaticResource MaterialDesignRaisedButton}" md:ButtonAssist.CornerRadius="8" HorizontalAlignment="Stretch" Height="44" IsDefault="True">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <md:PackIcon Kind="Connection" VerticalAlignment="Center" Margin="0,0,8,0" Visibility="{Binding IsBusy, Converter={StaticResource InverseBoolToVisibility}}"/>
                        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True" Width="18" Height="18" Margin="0,0,8,0" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
                        <TextBlock Text="CONNECT" VerticalAlignment="Center" FontWeight="Medium" Visibility="{Binding IsBusy, Converter={StaticResource InverseBoolToVisibility}}"/>
                        <TextBlock Text="Connecting..." VerticalAlignment="Center" FontWeight="Medium" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </ScrollViewer>
    </md:DialogHost>
</Window>
